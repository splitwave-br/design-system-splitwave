import { ReactNode } from "react";
type TProps = {
    children: ReactNode;
    showMenu?: boolean;
};
export declare const ThemePreview: ({ children, showMenu }: TProps) => import("react/jsx-runtime").JSX.Element;
export {};
