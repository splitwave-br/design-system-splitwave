import { TCell } from "../../../../../components/Table/types";
import { ReactNode } from "react";
type TProps = TCell & {
    children: ReactNode;
};
export declare const Card: ({ children }: TProps) => import("react/jsx-runtime").JSX.Element;
export {};
