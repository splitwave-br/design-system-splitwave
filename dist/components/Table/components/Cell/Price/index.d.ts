import { TCell } from '../../../../../components/Table/types';
type TProps = TCell & {
    children: number;
    hasHighlight?: boolean;
};
export declare const Price: ({ children, hasHighlight }: TProps) => import("react/jsx-runtime").JSX.Element;
export {};
