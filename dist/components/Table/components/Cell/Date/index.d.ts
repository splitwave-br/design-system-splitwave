import { TCell } from "../../../types";
type TProps = TCell & {
    children: string | Date;
};
export declare const Date: ({ children }: TProps) => import("react/jsx-runtime").JSX.Element;
export {};
