import { SelectProps } from "./types";
export declare const Select: <T>({ asPortal, name, options, prefix, placeholder, exceptionRef, enableDeselect, searchable, value, disabled, className, triggerClassname, menuContainerClassname, menuInnerClassname, keyExtractor, getValue, onChange, getLabel, renderItem, ...props }: SelectProps<T>) => import("react/jsx-runtime").JSX.Element;
